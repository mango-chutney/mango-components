import React from 'react';
import banner from '../banner';

const Home = () => (
  <div>
    <code>
      <pre>{banner}</pre>
    </code>
  </div>
);

export const component = Home;

export default component;
